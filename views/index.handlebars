<h1>Burger Time</h1>

<h2>Here are the options</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
            <li>
                {{burger_name}}
                <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
                    DEVOUR ME!
                </button>
            </li>
        {{/unless}}
    {{/each}}<!--need to finish comparing this to the cats activity-->
</ul>

<h2>Burgers You've Devoured</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            <li>
                {{burger_name}}
                <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
                    UNDEVOUR ME!
                </button>
            </li>
        {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger to the Menu</h2>

<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Have you devoured this burger?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Not yet!
  </div>

  <button type="submit">Add Burger</button>
</form>